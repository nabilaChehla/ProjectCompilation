INTEGER ROUTINE A1 (X,Y)
INTEGER A;
A=9;
A1=A;
ENDR

CHARACTER ROUTINE R(A,B)
INTEGER X = 5 , Z3 ;
X = 1;
Z3 = X;
R="MELI";
ENDR

PROGRAM HelloAcad
% Déclarations
REAL A=0,B = 9,C =5.0  ; 
REAL Y =5.3 ;
INTEGER S=666,E=7,F=8,I,J,K,temp=4;
CHARACTER P ; 
INTEGER TAB DIMENSION (9+ 99) ; 
REAL MAT DIMENSION (B+ 7.9 , 9) ;
REAL MAT1 DIMENSION (7,7.9+E) ;
INTEGER TAB3 DIMENSION (700) ;

% Instructions
MAT1(4,F)=6;
MAT1(E,4)=6;
%TAB3(A)=6;
TAB(8)=6;

P = "I";

IF(A.EQ.B) THEN
    A = A - B ;
    IF(((A.GT.(B + 1)).OR.(C.EQ.TRUE))) THEN A = A - B ;
        B = 30;
    ELSE
        A = A + B;
    ENDIF
ELSE A=B;
ENDIF

DOWHILE (Y.GT.B) 
    A = A - B ;
    IF(((A.GT.(B + 1)).OR.(C.EQ.TRUE))) THEN A = A - B ;
        B = 30;
    ELSE
        A = A + B;
    ENDIF
ENDDO;

DOWHILE (A.LT.B) 
  DOWHILE(B.GT.C)
   A= A+B;
  ENDDO;
   IF(((A.GT.(B + 1)).OR.(C.EQ.TRUE))) THEN A = A - B ;
        B = 30;
    ELSE
        A = A + B;
    ENDIF
ENDDO;
A=C;
A=Y;

END
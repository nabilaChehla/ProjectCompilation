% Test Quadruplets de condition + Boucle :_________________

INTEGER ROUTINE fonction1 (X,Y,MAT(20,20))
INTEGER A , B ; 
LOGICAL C ; 
A = 5 ; 
B = A ;
C = FALSE ; 
DOWHILE (A.LT.10)
   A = A + 1;
   IF(((A.GT.(B + 1)).OR.(C.EQ.TRUE))) THEN A = A - 5 ;
	MAT(0,0) =MAT(1,0) * A + (B * 7);
	B = 30*3;
   ELSE A = A + B;
   ENDIF
ENDDO;

fonction1 = A ; 
ENDR 

% Test Quadruplets de Read Write Equivalence:______________

REAL ROUTINE fonction2 (X,Y,Z)
INTEGER B  , A = -3; 
REAL R =1.1; 
INTEGER MAT DIMENSION (100,100) ;
%INTEGER MATNeg DIMENSION (5,R) ;
%INTEGER MATNeg DIMENSION (-1,3) ;
%INTEGER MATNeg DIMENSION (A,3) ;

READ(A); 
READ(MAT(5,2));
READ(B); 
WRITE("Bonjours",A/2+B,B*5);

EQUIVALENCE (Z, X, MAT(5,6)), (Y, X); 
fonction2 = B * X ; 
ENDR 
%___________________________________________________________

PROGRAM HelloAcad
% Déclarations
REAL A,B,C = 6.3  ; 
INTEGER Y , Z=600 ; 

INTEGER TAB DIMENSION (Z) ;
CHARACTER MAT DIMENSION (10,10) ; 
CHARACTER char *5;

% Test Quadruplets de Affectation + expression:
A = 5 ; 
Y = 50; 
B = A*2 + Y*Y+ 50 /6; 
C = 2.25; 
char="Hi";

% Test Quadruplets de CALL:--------------------
A =CALL fonction2(TAB(Y),A, C);

END
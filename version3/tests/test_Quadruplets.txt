% Test Quadruplets de condition + Boucle :_________________

INTEGER ROUTINE fonction1 (X,Y,Z)
INTEGER A , B ; 
LOGICAL C ; 
INTEGER MAT DIMENSION (X+ 50,Y*2) ;
A = 5 ; 
B = A ;

DOWHILE (A.LT.10)
   A = A + 1;
   IF(((A.GT.(B + 1)).OR.(C.EQ.TRUE))) THEN A = A - 5 ;
	MAT(A+1,B-1) = A + (B * 7);
	B = 30;
   ELSE A = A + B;
   ENDIF
ENDDO;

fonction1 = A ; 
ENDR 
% Test Quadruplets de Read Write Equivalence:______________

REAL ROUTINE fonction2 (X,Y,Z)
INTEGER B , A;
INTEGER MAT DIMENSION (100,100) ;

READ(A); 
READ(B); 
WRITE("Bonjours",A+2,B*5);

EQUIVALENCE (A, X, MAT(5,6)), (Y, X); 
fonction2 = B * X ; 
ENDR 
%___________________________________________________________

PROGRAM HelloAcad
% Déclarations
REAL A,B,C  ; 
INTEGER Y ; 
INTEGER TAB DIMENSION (10) ; 
CHARACTER MAT DIMENSION (10,10) ; 

% Test Quadruplets de Affectation + expression:
A = 5 ; 
B = A*2 + Y*Y+50 /6; 

% Test Quadruplets de CALL:--------------------
A =CALL fonction2(MAT(0,0)/2,A, C);

END